// Alert controller — Alert System Module — Phase 2.
// evaluateAndGenerateAlert() (SDD 6.4) runs in the analysis pipeline;
// this controller serves alert lists and status transitions
// (New -> Viewed -> Dismissed).
