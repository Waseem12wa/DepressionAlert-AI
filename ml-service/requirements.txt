# DepressionAlert AI - Python NLP/ML service dependencies.
# Only libraries explicitly named in the SRS/Proposal do the NLP/ML work
# (SRS CO-2): Python 3.11, scikit-learn, spaCy, NLTK.
scikit-learn>=1.5,<2.0
spacy>=3.7,<4.0
nltk>=3.8,<4.0
# Serving plumbing for the protected internal API (SRS 6.2) - same role
# as Express on the Node tier; not an NLP/ML stack choice.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.31
python-dotenv>=1.0,<2.0
# HTTP client for the Groq LLM scorer (OpenAI-compatible API)
requests>=2.32,<3.0
